/**
 * Indicates the height of the header bars, in rems.
 */
export const frameworkHeaderBarHeight = '2';

/**
 * The color to use for selected menu items.
 */
export const selectedMenuItemColor = '#0078d7';

/**
 * The text color scheme for the toolbars.
 */
export const toolbarTextColor = `color: #ffffff;
background-color: #000000;`;

/**
 * The default font-family for the framework.
 */
export const frameworkDefaultFontFamily = `font-family: 'Segoe UI Regular';`;

/**
 * Flexbox style with primary rows, centered on width and height of container.
 */
export const flexRowCenterCenter = `display: flex; 
justify-content: center; 
align-items: center;`;

/**
 * Flexbox style with primary rows, centered vertically in the container, with maximum horizontal space between items.
 */
export const flexRowJustifyBetweenAlignItemsCenter = `display: flex; 
justify-content: space-between;
align-items: center;`;

/**
 * Style to set font size to 12px
 */
export const fontSize12Px = `font-size: 12px;`;

/**
 * Flexbox style with primary rows, where items are centered vertically in the container.
 */
export const flexRowAlignItemsCenter = `display:flex;
align-items: center;`;

/**
 * Default button style, which removes borders and sets the cursor to a pointer.
 */
export const defaultButton = `border: none;
cursor: pointer;`;
